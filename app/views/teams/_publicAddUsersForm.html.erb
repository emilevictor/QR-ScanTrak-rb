You have <%= pluralize(@numberLeft, 'slot') %> left (out of <%= Settings.maxTeamMembers %>)


<%= form_tag("/teams/addPlayerToMyTeam", :method => "post") do %>
<%= label_tag(:email, "Enter the email address of a registered player") %><br />
<%= text_field_tag(:email) %>
<%= hidden_field_tag(:team_id, @team.id) %>

<%= submit_tag("Add User to #{@team.name}") %>
<% end %>
